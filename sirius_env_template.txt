# Sirius Star Lab — Environment Variables for Private Server
# Copy this to /opt/sirius/.env and fill in all values

# Database — point to your new PostgreSQL instance
DATABASE_URL=postgresql://sirius:YOUR_PASSWORD@localhost:5432/siriusdb

# AI / OpenRouter
OPENROUTER_API_KEY=get_from_replit_secrets

# Stripe payments
STRIPE_SECRET_KEY=get_from_replit_secrets
STRIPE_PUBLISHABLE_KEY=get_from_replit_secrets
STRIPE_WEBHOOK_SECRET=get_from_replit_secrets

# Object storage (if using Replit Object Storage — migrate to S3/R2 or local)
DEFAULT_OBJECT_STORAGE_BUCKET_ID=get_from_replit_secrets
PRIVATE_OBJECT_DIR=get_from_replit_secrets
PUBLIC_OBJECT_SEARCH_PATHS=get_from_replit_secrets

# Star Lab PIN
LAB_PIN=169323

# Server config
NODE_ENV=production
PORT=3000
